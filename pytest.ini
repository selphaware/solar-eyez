[pytest]
minversion = 7.0
addopts = -ra -q
# Directory where tests are located
 testpaths =
    tests
# Naming pattern for test files
python_files = test_*.py
# Enable type-checking friendly markers
markers =
    integration: mark an integration test that may take longer to run
    performance: mark a performance/benchmark test
